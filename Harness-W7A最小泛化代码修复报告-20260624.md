# W7-A Harness 最小泛化 — Code Fix Report

> **taskId**：W7-A Harness 最小泛化
> **阶段**：code-fix → code-fix-review（下次 review 入口）
> **生成日期**：2026-06-24
> **fix owner**：claude-implementer-minimax (A 窗口)
> **来源**：B 窗口 `Harness-W7A最小泛化CodeReview意见-20260624.md`（P0=0, P1=1, P2=5；Decision: pass）
> **基线**：方案 v2 + FixReport + 代码实施报告 v1

---

## 修复结论

| 优先级 | Finding 数 | 已 fixed | accepted-unfixed | false-positive |
|--------|-----------|----------|------------------|----------------|
| P0 | 0 | 0 | 0 | 0 |
| P1 | 1 | **1** | 0 | 0 |
| P2 | 5 | **5** | 0 | 0 |
| **合计** | **6** | **6** | **0** | **0** |

**6/6 fixed，0 open finding。**

---

## Fix Mapping（Harness gate 兼容格式）

### Fix Mapping

| Finding | Status | 修复文件 | 验证 |
| --- | --- | --- | --- |
| HW7A-CR-P1-001 | fixed | `scripts/harness.js` L52-95（`loadWorkflowConfig`） | `pnpm harness init W8-A --force` 抛错：`No task-specific config found for W8-A (expected: .../weekly-canvas-task-W8-A.json). Default config is for W6-A. Create workflows/weekly-canvas-task-W8-A.json first.`；W6-A/W7-A 不受影响 |
| HW7A-CR-P2-001 | fixed | `Harness-W7A最小泛化代码实施报告-20260624.md` §8 | `grep -c "no-op cp\|cp /.*\\.md \\\\\\\\$" ...实施报告-20260624.md` = 0（删除了 source=dest 的 cp） |
| HW7A-CR-P2-002 | fixed | `tests/harness.test.js` L2260-2296（`before()`）+ L2543-2560（生产 config 结构验证） | `pnpm test` → 测试 #17 验证生产 `weekly-canvas-task-W7-A.json` taskId/taskTitle/subtasks/stages/reportRoot 5 项必填字段；W7-A 测试 `before()` 从生产文件派生而非手写 |
| HW7A-CR-P2-003 | fixed | `scripts/harness.js` L897-908（`_firstRunDefaultsForInit` 注释） | 注释说明该变量与 L925+ `firstRunDefaults` 用途区分 |
| HW7A-CR-P2-004 | fixed | `scripts/harness.js` L908-913（IIFE fallback 防御） | empty `subtasks` → 抛 `No subtasks defined in workflow config for W7-A`（非 TypeError）；新测试 #18 覆盖 |
| HW7A-CR-P2-005 | fixed | `scripts/harness.js` L51-54（config 命名约定注释） | 注释说明命名约定 `workflows/weekly-canvas-task-{taskId}.json` |

---

## 改动文件清单

| 文件 | 类型 | 行数变化 |
|---|---|---|
| `scripts/harness.js` | 修改 | +20 / -3 |
| `tests/harness.test.js` | 修改 | +75 / -75（净：+20 行 describe 块内容替换 + 55 行新测试） |
| `Harness-W7A最小泛化代码实施报告-20260624.md` | 修改 | 文档 §8 删 no-op cp（+7 / -5） |
| `Harness-W7A最小泛化代码修复报告-20260624.md` | **新建** | 本报告 |

**未触碰文件**：
- ✅ `workflows/weekly-canvas-task-W7-A.json`（生产 config 0 修改 — 测试从该文件派生）
- ✅ `workflows/weekly-canvas-task.json`（W6-A 0 回归）
- ✅ `runs/W6-A/status.json`（未触碰）
- ✅ `runs/W6-A/outputs/**` / `runs/W6-A/prompts/**` / `runs/W6-A/handoffs/**`（未触碰）
- ✅ `/Users/admin/project/ai/work/HEXAI`（HEXAI 业务代码仓库 0 修改）
- ✅ `.claude/settings.json` / API key / token / cookie（未读取）
- ✅ git commit / push（未执行）

---

## 关键改动详情

### 1. HW7A-CR-P1-001：未知 taskId throw（harness.js L52-95）

**修复前**：
```js
const taskConfigPath = path.join(HARNESS_ROOT, 'workflows', `weekly-canvas-task-${taskId}.json`);
const defaultConfigPath = path.join(HARNESS_ROOT, 'workflows', 'weekly-canvas-task.json');
const jsonPath = fs.existsSync(taskConfigPath) ? taskConfigPath : defaultConfigPath;
// ... load and parse ...
if (_workflowConfig.taskId !== taskId && taskId) {
  console.error(`Warning: Config taskId "${_workflowConfig.taskId}" differs from requested "${taskId}". Using config.`);
}
```

**修复后**：
```js
// HW7A-CR-P2-005: config 文件命名约定
//   workflows/weekly-canvas-task-{taskId}.json
const taskConfigPath = path.join(HARNESS_ROOT, 'workflows', `weekly-canvas-task-${taskId}.json`);
const defaultConfigPath = path.join(HARNESS_ROOT, 'workflows', 'weekly-canvas-task.json');
const usedFallback = !fs.existsSync(taskConfigPath);
const jsonPath = usedFallback ? defaultConfigPath : taskConfigPath;
// ... load and parse ...
// HW7A-CR-P1-001: 当 taskId-specific config 不存在且 default config.taskId !== taskId 时
// 必须 throw 而非仅 warning（避免 W8-A 静默使用 W6-A config 创建错误 status.json）
if (usedFallback && parsed.taskId !== taskId && taskId) {
  throw new Error(
    `No task-specific config found for ${taskId} (expected: ${taskConfigPath}). ` +
    `Default config is for ${parsed.taskId}. ` +
    `Create workflows/weekly-canvas-task-${taskId}.json first.`
  );
}
```

**防御性编程**：
- `pnpm harness init W6-A` → 走 fallback → `default.taskId === 'W6-A' === taskId` → 不 throw ✅
- `pnpm harness init W7-A` → taskId-specific config 存在 → `usedFallback === false` → 不 throw ✅
- `pnpm harness init W8-A` → 无 W8-A config → 走 fallback → `default.taskId === 'W6-A' !== 'W8-A'` → **throw** ✅

### 2. HW7A-CR-P2-004：空 subtasks 防御（harness.js L908-913）

**修复前**：
```js
const _defaultFromSubtask = (() => {
  for (const s of getSubtasksForTask()) {
    if (!_importedCompleted.has(s.id)) return s.id;
  }
  return getSubtasksForTask()[0].id;  // ← TypeError if subtasks=[]
})();
```

**修复后**：
```js
const _defaultFromSubtask = (() => {
  for (const s of getSubtasksForTask()) {
    if (!_importedCompleted.has(s.id)) return s.id;
  }
  // HW7A-CR-P2-004: subtasks 为空数组时给出明确错误（而非 TypeError）
  const first = getSubtasksForTask()[0];
  if (!first) {
    throw new Error(`No subtasks defined in workflow config for ${taskId}`);
  }
  return first.id;
})();
```

### 3. HW7A-CR-P2-005：config 命名约定注释（harness.js L51-54）

```js
// HW7A-CR-P2-005: config 文件命名约定
//   workflows/weekly-canvas-task-{taskId}.json
//   e.g., weekly-canvas-task-W7-A.json, weekly-canvas-task-W8-A.json
const taskConfigPath = ...
```

### 4. HW7A-CR-P2-003：变量注释（harness.js L895-908）

```js
// HW7A-CR-P2-003: `_firstRunDefaultsForInit` 仅用于计算 fromSubtask 默认值；
// 与下方 L925+ 的 `firstRunDefaults`（用于 residual risk + console.log）独立。
const _firstRunDefaultsForInit = (getWorkflowConfig().firstRunDefaults) || {};
```

### 5. HW7A-CR-P2-002：测试 config 派生生产文件（tests/harness.test.js）

**修复前**（约 70 行手写 JSON config）：
```js
before(() => {
  fs.writeFileSync(w7ConfigPath, JSON.stringify({
    "$schema": "...",
    "taskId": "W7-A",
    // ... 完全手写的 W7-A config ...
  }, null, 2), 'utf-8');
});
```

**修复后**（从生产派生）：
```js
// HW7A-CR-P2-002: srcHarness declared at module scope so describe-level
// before() hooks (notably W7-A) can read production configs
const srcHarness = path.resolve(import.meta.dirname, '..');

before(() => {
  const productionConfigPath = path.join(srcHarness, 'workflows', 'weekly-canvas-task-W7-A.json');
  if (!fs.existsSync(productionConfigPath)) {
    throw new Error(`Test precondition failed: production ${productionConfigPath} not found`);
  }
  const productionConfig = JSON.parse(fs.readFileSync(productionConfigPath, 'utf-8'));
  const testConfig = JSON.parse(JSON.stringify(productionConfig));
  // 重写路径字段以指向 TEST_ROOT
  testConfig.reportRoot = path.join(REVIEW_ROOT, 'W7');
  testConfig.codeRepo = CODE_REPO;
  testConfig.reviewRoot = REVIEW_ROOT;
  if (testConfig.requiredPlaybooks) {
    for (const key of Object.keys(testConfig.requiredPlaybooks)) {
      testConfig.requiredPlaybooks[key] = testConfig.requiredPlaybooks[key].map(p =>
        path.join(REVIEW_ROOT, 'ReviewPlaybooks', path.basename(p))
      );
    }
  }
  const w7ConfigPath = path.join(HARNESS_DIR, 'workflows', 'weekly-canvas-task-W7-A.json');
  fs.writeFileSync(w7ConfigPath, JSON.stringify(testConfig, null, 2), 'utf-8');
});
```

**新增测试**（describe 块外）：
```js
describe('W7-A production config structure validation', () => {
  it('production workflows/weekly-canvas-task-W7-A.json has required fields', () => {
    const cfg = JSON.parse(fs.readFileSync(productionConfigPath, 'utf-8'));
    assert.equal(cfg.taskId, 'W7-A');
    assert.ok(cfg.taskTitle && cfg.taskTitle.trim().length > 0);
    assert.ok(Array.isArray(cfg.subtasks) && cfg.subtasks.length > 0);
    assert.ok(cfg.stages.length === 10);
    assert.ok(cfg.reportRoot.endsWith('/W7'));
  });
});
```

### 6. HW7A-CR-P2-001：实施报告 §8 删 no-op cp

**修复前**（含 source=dest cp）：
```bash
# 接入已落地方案（Harness 期望路径 == 已落地路径，无需 cp）
cp /Users/admin/project/ai/review/W7/W7-A/W7-A画布可演示交互闭环实施计划-20260624.md \
   /Users/admin/project/ai/review/W7/W7-A/W7-A画布可演示交互闭环实施计划-20260624.md
```

**修复后**（仅注释说明，无 cp）：
```bash
# 情形 A：路径已对齐 → 无需 cp；直接 check
#   Harness 期望: /review/W7/W7-A/W7-A画布可演示交互闭环实施计划-20260624.md
#   已落地:     /review/W7/W7-A/W7-A画布可演示交互闭环实施计划-20260624.md (YYYYMMDD 一致)
pnpm harness check W7-A
```

### 7. 配套测试更新（V2.4 schemaVersion 3 migration）

迁移测试原用 `W6-A-MIGRATE-TEST` taskId 隔离 → 修复 P1 后会 throw。改为 `taskId = 'W6-A'`（TEST_ROOT 已隔离，不污染真实 runs/W6-A/status.json），添加注释说明调整原因。

---

## 新增 / 修改测试用例

| # | 测试名 | 覆盖 Finding |
|---|---|---|
| 17 | `production workflows/weekly-canvas-task-W7-A.json has required fields` | HW7A-CR-P2-002（Option B：验证生产 config 结构） |
| 18 | `HW7A-CR-P2-004: empty subtasks config throws clear error` | HW7A-CR-P2-004 |
| 19 | `HW7A-CR-P1-001: init W8-A (no taskId-specific config) throws clear error` | HW7A-CR-P1-001 |
| 调整 | `should migrate old status without schemaVersion` | 改用 `W6-A` taskId（HW7A-CR-P1-001 兼容） |

---

## 测试结果

```
$ pnpm test
# tests 159
# suites 55
# pass 159
# fail 0
# cancelled 0
# duration_ms 12128
Exit code: 0
```

| 指标 | 值 | 变化 |
|---|---|---|
| 总测试数 | **159** | +3（修复后从 156 增至 159：测试 #17、#18、#19） |
| 通过 | **159** | +3 |
| 失败 | **0** | 不变 |
| W6-A 回归 | ✅ | 138 个 W6-A 测试 0 回归 |
| W7-A 新增 | ✅ | 19 个 W7-A 测试全通过 |
| 新测试 | ✅ | 3 个新测试全通过 |

---

## W6-A 隔离验证

| 维度 | 状态 |
|---|---|
| `runs/W6-A/status.json` mtime | ✅ 未触碰 |
| `runs/W6-A/outputs/**` | ✅ 未触碰 |
| `runs/W6-A/prompts/**` | ✅ 未触碰 |
| `runs/W6-A/handoffs/**` | ✅ 未触碰 |
| `workflows/weekly-canvas-task.json` | ✅ 仅 code-fix 不修改此文件 |

---

## 演示版命令链路验证

| 命令 | 预期 | 验证 |
|---|---|---|
| `pnpm harness init W6-A` | ✅ 成功（fallback config + taskId 匹配） | 测试 #9 / #10 / #11 |
| `pnpm harness init W7-A` | ✅ 成功（taskId-specific config） | 测试 #1-#3 |
| `pnpm harness init W8-A --force` | ❌ exit 1 + 明确错误 | **测试 #19**（新增） |
| `pnpm harness step W7-A`（含已落地报告） | ✅ 推到 plan-review | 测试 #15 |

---

## 风险与已落地防御

| 风险 | 防御措施 |
|---|---|
| W8-A 静默使用 W6-A config | HW7A-CR-P1-001 throw（明确拒绝 + 提示创建 config） |
| W7-A config 误改（taskTitle 缺失等） | 测试 #17 验证生产文件 5 项必填字段；测试 `before()` 派生自生产文件（不手写） |
| subtasks 空数组导致 TypeError | HW7A-CR-P2-004 throw `No subtasks defined` |
| `_firstRunDefaultsForInit` vs `firstRunDefaults` 混淆 | HW7A-CR-P2-003 注释区分 |
| 未来 W8-A config 命名漂移 | HW7A-CR-P2-005 注释说明命名约定 |
| 迁移测试因 P1 修复而 throw | 改用 `W6-A` taskId（TEST_ROOT 隔离，无副作用） |

---

## 后续路径

| 步骤 | Owner | 产物 |
|---|---|---|
| 1. 本报告 review | B 窗口 | code-fix-review 意见 |
| 2. 修复批准 | codex-overall-reviewer | 进入 W7-A Harness V2 验收 |
| 3. W7-A 流程推进 | A/B/C 窗口 | `pnpm harness {current,next,check,advance,step} W7-A` 实际跑通 |
| 4. W8-A / W9-A | 未来窗口 | 复制 W7-A JSON + 改 4 字段；新建 `weekly-canvas-task-W8-A.json` |

---

## 验收 checklist

| # | 项目 | 状态 |
|---|---|---|
| 1 | HW7A-CR-P1-001 修复（未知 taskId throw） | ✅ 测试 #19 覆盖 |
| 2 | HW7A-CR-P2-001 修复（实施报告 §8 删 no-op cp） | ✅ |
| 3 | HW7A-CR-P2-002 修复（测试 config 派生生产 + 生产 config 结构验证） | ✅ 测试 #17 |
| 4 | HW7A-CR-P2-003 修复（变量注释） | ✅ |
| 5 | HW7A-CR-P2-004 修复（空 subtasks 防御） | ✅ 测试 #18 |
| 6 | HW7A-CR-P2-005 修复（config 命名约定注释） | ✅ |
| 7 | `pnpm test` exit 0 | ✅ 159/0 |
| 8 | W6-A 0 回归 | ✅ |
| 9 | W7-A 闭环仍可用 | ✅ |
| 10 | 不修改 HEXAI | ✅ |
| 11 | 不修改 runs/W6-A | ✅ |
| 12 | 不 commit/push | ✅ |

---

**作者签名**：claude-implementer-minimax (A 窗口)
**日期**：2026-06-24
**下一步**：B 窗口 code-fix-review（`hexai-code-fix-review` skill）或 codex-overall-reviewer 验收